--7MAIN CODE

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TextChatService = game:GetService("TextChatService")
local VIM = game:GetService("VirtualInputManager")

local lp = Players.LocalPlayer
local targetName = getgenv().TargetPlayer
local OWNER_NAME = "devtrav23"

-- ===== SETTINGS =====
local autoDefend = false
local lastDefendTime = 0

-- ===== CHAT =====
local function say(msg)
	if not msg then return end
	local ch = TextChatService.ChatInputBarConfiguration.TargetTextChannel
	if ch then ch:SendAsync(msg) end
end

-- ===== EXPLOIT =====
pcall(function()
	sethiddenproperty(lp, "SimulationRadius", math.huge)
	sethiddenproperty(lp, "MaxSimulationRadius", math.huge)
end)

-- ===== OFFSETS =====
local RIGHT_OFFSET = Vector3.new(-3, 2.5, 4)
local LEFT_OFFSET  = Vector3.new(3, 2.5, 4)
local FRONT_OFFSET = Vector3.new(0, 0.1, -5.5)
local VANISH_OFFSET = Vector3.new(0, -600, 0)

local STAND_OFFSET = (getgenv().standPosition == "left") and LEFT_OFFSET or RIGHT_OFFSET

local vanished = true
local lowHPSaid = false
local isAttacking = false

local char, hrp, hum, bp, bg, idleTrack, animator

-- ===== RECREATE ON RESET =====
local function setupCharacter()
	char = lp.Character or lp.CharacterAdded:Wait()
	hrp = char:WaitForChild("HumanoidRootPart")
	hum = char:WaitForChild("Humanoid")
	hum.PlatformStand = true

	animator = hum:WaitForChild("Animator")

	local animateScript = char:FindFirstChild("Animate")
	if animateScript then animateScript.Disabled = true end

	-- Forces
	if bp then bp:Destroy() end
	if bg then bg:Destroy() end

	bp = Instance.new("BodyPosition", hrp)
	bp.MaxForce = Vector3.new(1e9, 1e9, 1e9)
	bp.P = 180000
	bp.D = 1800

	bg = Instance.new("BodyGyro", hrp)
	bg.MaxTorque = Vector3.new(1e9, 1e9, 1e9)
	bg.P = 1e6
	bg.D = 150

	-- Noclip
	RunService.Stepped:Connect(function()
		for _, v in ipairs(char:GetDescendants()) do
			if v:IsA("BasePart") then v.CanCollide = false end
		end
	end)
end

setupCharacter()

lp.CharacterAdded:Connect(function()
	task.wait(0.5)
	setupCharacter()
end)

-- ===== IDLE ANIMATION =====
local idleAnim = Instance.new("Animation")
idleAnim.AnimationId = "rbxassetid://17124061663"

local function playIdle()
	if vanished or isAttacking then return end
	if idleTrack and idleTrack.IsPlaying then return end
	if idleTrack then idleTrack:Destroy() end
	
	idleTrack = animator:LoadAnimation(idleAnim)
	idleTrack.Priority = Enum.AnimationPriority.Action4
	idleTrack.Looped = true
	idleTrack:Play()
end

local function stopIdle()
	if idleTrack then idleTrack:Stop() end
end

-- ===== SKILLS SETUP =====
local keyMap = {
	[getgenv().SkillWord1 and getgenv().SkillWord1:lower()] = Enum.KeyCode.One,
	[getgenv().SkillWord2 and getgenv().SkillWord2:lower()] = Enum.KeyCode.Two,
	[getgenv().SkillWord3 and getgenv().SkillWord3:lower()] = Enum.KeyCode.Three,
	[getgenv().SkillWord4 and getgenv().SkillWord4:lower()] = Enum.KeyCode.Four
}

local skillQuotes = { ... } 
local cooldowns = { ... }   
local lastUsed = {}
local saidCD = {}

local function press(k)
	VIM:SendKeyEvent(true, k, false, game)
	task.wait(0.05)
	VIM:SendKeyEvent(false, k, false, game)
end

-- ===== AUTO DEFEND (Improved) =====
local function getRandomAvailableSkill()
	local available = {}
	local now = tick()

	for word, key in pairs(keyMap) do
		if word then
			local cd = cooldowns[word] or 0
			local last = lastUsed[word] or 0
			if now - last >= cd then
				table.insert(available, {word = word, key = key})
			end
		end
	end

	if #available == 0 then return nil end
	return available[math.random(1, #available)]
end

local function autoDefendAttack(attackerHRP)
	if not attackerHRP or isAttacking then return end
	if not hrp then return end
	
	isAttacking = true
	stopIdle()
	
	-- Go in front of attacker
	local attackerCFrame = attackerHRP.CFrame
	ACTIVE_OFFSET = FRONT_OFFSET
	bp.Position = attackerCFrame:PointToWorldSpace(FRONT_OFFSET)
	bg.CFrame = attackerCFrame
	
	task.wait(0.4) -- small delay to move into position
	
	local skill = getRandomAvailableSkill()
	
	if skill then
		lastUsed[skill.word] = tick()
		press(skill.key)
	end
	
	task.delay(2.5, function()
		isAttacking = false
		ACTIVE_OFFSET = STAND_OFFSET
		
		-- Return to owner
		local target = getTargetHRP()
		if target and not vanished then
			bp.Position = target.CFrame:PointToWorldSpace(STAND_OFFSET)
			bg.CFrame = target.CFrame
			playIdle()
		end
	end)
end

-- ===== OWNER DEFEND SETUP =====
local ownerHumConnection

local function getClosestAttacker(ownerHRP)
	local closest = nil
	local minDist = 6
	
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr.Character and plr.Name ~= OWNER_NAME and plr ~= lp then
			local aHRP = plr.Character:FindFirstChild("HumanoidRootPart")
			if aHRP then
				local dist = (ownerHRP.Position - aHRP.Position).Magnitude
				if dist < minDist then
					minDist = dist
					closest = aHRP
				end
			end
		end
	end
	return closest
end

local function setupOwnerDefend()
	local owner = Players:FindFirstChild(OWNER_NAME)
	if not owner then return end
	
	if ownerHumConnection then ownerHumConnection:Disconnect() end
	
	local function connectHealth()
		if owner.Character then
			local ohum = owner.Character:FindFirstChild("Humanoid")
			if ohum then
				ownerHumConnection = ohum.HealthChanged:Connect(function(health)
					if autoDefend and health < ohum.MaxHealth and tick() - lastDefendTime > 2.5 then
						lastDefendTime = tick()
						local ownerHRP = owner.Character:FindFirstChild("HumanoidRootPart")
						if ownerHRP then
							local attacker = getClosestAttacker(ownerHRP)
							if attacker then
								autoDefendAttack(attacker)
							end
						end
					end
				end)
			end
		end
	end
	
	connectHealth()
	
	owner.CharacterAdded:Connect(function()
		task.wait(0.8)
		connectHealth()
	end)
end

setupOwnerDefend()

-- ===== TARGET & CHAT HOOK =====
local function getTargetHRP()
	local p = Players:FindFirstChild(targetName)
	return p and p.Character and p.Character:FindFirstChild("HumanoidRootPart")
end

local function hook(p)
	p.Chatted:Connect(function(m)
		m = m:lower():gsub("%s+", "")

		if m == ".ad" then
			autoDefend = not autoDefend
			return
		end

		if m == (getgenv().SummonWord or "summon"):lower() and hum.Health/hum.MaxHealth > 0.25 then
			vanished = false
			lowHPSaid = false
			isAttacking = false
			say(math.random(1,2)==1 and getgenv().SummonQuote1 or getgenv().SummonQuote2)
			task.wait(0.3)
			playIdle()
			return
		end

		if m == (getgenv().DismissWord or "dismiss"):lower() then
			vanished = true
			isAttacking = false
			stopIdle()
			say(math.random(1,2)==1 and getgenv().DismissQuote1 or getgenv().DismissQuote2)
			return
		end

		if keyMap[m] and not vanished then
			local now = tick()
			if now - (lastUsed[m] or 0) < (cooldowns[m] or 0) then
				if not saidCD[m] then
					saidCD[m] = true
					say(getgenv().CooldownQuote)
				end
				return
			end

			lastUsed[m] = now
			saidCD[m] = false

			isAttacking = true
			stopIdle()
			ACTIVE_OFFSET = FRONT_OFFSET

			say(skillQuotes[m])
			press(keyMap[m])

			task.delay(1.8, function()
				isAttacking = false
				if not vanished then
					ACTIVE_OFFSET = STAND_OFFSET
					playIdle()
				end
			end)
		end
	end)
end

-- Connect hooks
for _, p in ipairs(Players:GetPlayers()) do
	if p.Name == targetName then hook(p) end
end

Players.PlayerAdded:Connect(function(p)
	if p.Name == targetName then hook(p) end
end)

-- ===== MAIN LOOP =====
RunService.Heartbeat:Connect(function()
	local t = getTargetHRP()
	if not t or not hrp then return end

	if hum.Health / hum.MaxHealth <= 0.25 then
		if not lowHPSaid then
			lowHPSaid = true
			say(getgenv().LowHPQuote)
		end
		vanished = true
		isAttacking = false
		stopIdle()
	end

	if vanished then
		bp.Position = t.Position + VANISH_OFFSET
		bg.CFrame = t.CFrame
	elseif not isAttacking then
		bp.Position = t.CFrame:PointToWorldSpace(STAND_OFFSET)
		bg.CFrame = t.CFrame
	end
end)

RunService.Heartbeat:Connect(function()
	if not vanished and hum:GetState() == Enum.HumanoidStateType.Freefall then
		hum:ChangeState(Enum.HumanoidStateType.RunningNoPhysics)
	end
end)

hum.Died:Connect(function()
	if idleTrack then idleTrack:Stop() end
end)
